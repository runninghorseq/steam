meta {
  name: Filter Counts
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/accounts/filter-counts
  body: none
  auth: none
}

headers {
  X-Dashboard-Token: {{token}}
}

docs {
  Record count per filter chip + total.
}
